app.config(function($mdThemingProvider, $mdIconProvider, ChartJsProvider) {
	$mdThemingProvider.theme('default')
	.primaryPalette('green', {
		'default': '500'
	})
	.accentPalette('red', {
		'default': 'A200'
	});
});
